version: '3.8'

services:
  mysql_messaging:
    image: mysql:8.0
    container_name: mysql_messaging
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: crm_password
      MYSQL_DATABASE: messaging_db
      MYSQL_USER: crm_user
      MYSQL_PASSWORD: crm_password
    ports:
      - "3309:3306"
    volumes:
      - mysql_messaging_data:/var/lib/mysql
    networks:
      - crm_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "crm_user", "-pcrm_password"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  messaging_service:
    build: .
    container_name: messaging_service
    environment:
      - DB_NAME=messaging_db
      - DB_USER=crm_user
      - DB_PASSWORD=crm_password
      - DB_HOST=mysql_messaging
      - DB_PORT=3306
    depends_on:
      mysql_messaging:
        condition: service_healthy
    ports:
      - "8003:8000"
    volumes:
      - ../../certs:/certs-in:ro
    networks:
      - crm_network
    restart: unless-stopped

volumes:
  mysql_messaging_data:

networks:
  crm_network:
    external: true
    name: crm_network